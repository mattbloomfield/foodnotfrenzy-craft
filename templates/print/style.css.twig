<style type="text/css">
                          /* Base styles for both screen and print */
                      body {
                          font-family: 'Georgia', serif;
                          line-height: 1.5;
                          color: #333;
                          margin: 0;
                          padding: 0;
                      }

.print-container {
    max-width: 800px;
    margin: 0 auto;
}

/* Clear typography and hierarchy */
.recipe-title {
    font-size: 24pt;
    font-weight: bold;
    margin-bottom: 8pt;
    text-align: center;
    font-family: 'Arial', sans-serif;
}

.recipe-meta {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 20px;
    margin: 15px 0 25px;
    padding-bottom: 15px;
    border-bottom: 1px solid #ddd;
}

.meta-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    font-family: 'Arial', sans-serif;
}

.meta-label {
    font-size: 9pt;
    text-transform: uppercase;
    color: #666;
}

.meta-value {
    font-size: 11pt;
    font-weight: bold;
}

.recipe-description {
    font-style: italic;
    margin-bottom: 25px;
    text-align: center;
    max-width: 80%;
    margin-left: auto;
    margin-right: auto;
}

.recipe-image {
    text-align: center;
    margin-bottom: 30px;
}

.recipe-image img {
    max-width: 250px;
    max-height: 250px;
    border-radius: 5px;
}

/* Section styling */
.recipe-section {
    margin-bottom: 30px;
    break-inside: avoid;
}

h2 {
    font-family: 'Arial', sans-serif;
    font-size: 16pt;
    margin-top: 25pt;
    margin-bottom: 10pt;
    border-bottom: 1.5pt solid #c86b6b;
    padding-bottom: 5pt;
    color: #8e6e53;
}

h3 {
    font-family: 'Arial', sans-serif;
    font-size: 14pt;
    margin-top: 15pt;
    margin-bottom: 8pt;
    color: #8e6e53;
}

/* Ingredients styling */
.ingredients-list {
    list-style-type: none;
    padding-left: 0;
    margin-bottom: 20px;
    column-count: 2;
    column-gap: 40px;
}

.ingredient-item {
    margin-bottom: 10px;
    break-inside: avoid;
    position: relative;
    padding-left: 20px;
}

.ingredient-item:before {
    content: "•";
    position: absolute;
    left: 0;
    color: #c86b6b;
    font-weight: bold;
}

.quantity {
    font-weight: bold;
}

.optional-note {
    font-style: italic;
    color: #666;
    margin-left: 5px;
}

/* Instructions styling */
.instructions-list {
    padding-left: 25px;
    margin-top: 0;
}

.instruction-item {
    margin-bottom: 15px;
    break-inside: avoid;
    position: relative;
}

.instruction-number {
    position: absolute;
    left: -25px;
    font-weight: bold;
    color: #c86b6b;
}

/* Footer styles */
.print-footer {
    margin-top: 40px;
    padding-top: 15px;
    border-top: 1px solid #ddd;
    display: flex;
    align-items: center;
    justify-content: space-between;
    font-size: 9pt;
    color: #666;
}

.source-url {
    flex-grow: 1;
}

.qrcode {
    margin-left: 20px;
    flex-shrink: 0;
}

/* Screen-only styles */
@media screen {
    body {
        background-color: #f5f0e1;
        padding: 20px;
    }

    .print-container {
        background: white;
        padding: 40px;
        box-shadow: 0 0 15px rgba(0,0,0,0.1);
        border-radius: 8px;
    }

    .screen-controls {
        max-width: 800px;
        margin: 0 auto 20px;
        display: flex;
        justify-content: space-between;
    }

    .btn {
        background: #c86b6b;
        color: white;
        border: none;
        padding: 10px 20px;
        border-radius: 4px;
        cursor: pointer;
        font-family: 'Arial', sans-serif;
        text-decoration: none;
        display: inline-block;
    }

    .btn:hover {
        background: #8e6e53;
    }

    .back-btn {
        background: #f5f0e1;
        color: #8e6e53;
        border: 1px solid #8e6e53;
    }
    .back-btn:hover {
        background: #8e6e53;
        color: white;
    }
}

/* Print-specific styles */
@media print {
    @page {
        size: letter;
        margin: 0.5in;
    }

    body {
        font-size: 12pt;
        line-height: 1.5;
        color: black;
        background: white;
    }

    .screen-controls,
    header,
    footer,
    .site-header,
    .site-footer,
    .no-print {
        display: none !important;
    }

    .print-container {
        max-width: 100%;
        margin: 0;
        padding: 0;
    }

    /* Better page breaks */
    h2, h3 {
        break-after: avoid;
    }

    .recipe-section {
        break-inside: avoid;
    }

    .ingredients-list {
        break-inside: avoid;
    }

    /* Override column layout for small recipes */
    @media (max-width: 5in) {
        .ingredients-list {
            column-count: 1;
        }
    }
}
</style>