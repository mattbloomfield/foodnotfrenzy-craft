<style type="text/css">
                          /* --- Core Variables & Reset --- */
                      :root {
                          --primary-color: #c86b6b;   /* Terracotta */
                          --accent-color: #8e6e53;    /* Muted Brown */
                          --text-dark: #2d2d2d;
                          --text-light: #666;
                          --border-light: #eee;
                      }

body {
    font-family: 'Georgia', 'Times New Roman', serif;
    line-height: 1.6;
    color: var(--text-dark);
    margin: 0;
    padding: 0;
}

.print-container {
    max-width: 850px;
    margin: 0 auto;
}

/* --- Typography & Header --- */
.recipe-title {
    font-family: 'Arial', 'Helvetica Neue', sans-serif;
    font-size: 28pt;
    font-weight: 800;
    margin-bottom: 4pt;
    text-align: center;
    color: var(--text-dark);
    text-transform: uppercase;
    letter-spacing: -1px;
}

.recipe-description {
    font-style: italic;
    font-size: 11pt;
    color: var(--text-light);
    margin: 0 auto 25px;
    text-align: center;
    max-width: 85%;
    line-height: 1.4;
}

.recipe-meta {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 30px;
    margin: 20px 0;
    padding: 15px 0;
    border-top: 1px solid var(--border-light);
    border-bottom: 1px solid var(--border-light);
}

.meta-item {
    text-align: center;
    font-family: 'Arial', sans-serif;
}

.meta-label {
    font-size: 8pt;
    text-transform: uppercase;
    letter-spacing: 1px;
    color: var(--text-light);
    display: block;
    margin-bottom: 2px;
}

.meta-value {
    font-size: 11pt;
    font-weight: bold;
    color: var(--accent-color);
}

/* --- Media/Images --- */
.recipe-image {
    text-align: center;
    margin-bottom: 30px;
}

.recipe-image img {
    max-width: 300px;
    height: auto;
    border-radius: 8px;
    /* Clean look for print */
    filter: contrast(1.05);
}

/* --- Section Styling --- */
h2 {
    font-family: 'Arial', sans-serif;
    font-size: 16pt;
    text-transform: uppercase;
    letter-spacing: 1px;
    margin-top: 30pt;
    margin-bottom: 12pt;
    color: var(--accent-color);
    display: flex;
    align-items: center;
}

h2:after {
    content: "";
    flex: 1;
    margin-left: 15px;
    height: 1px;
    background: var(--border-light);
}

/* --- Ingredients (with Checkboxes) --- */
.ingredients-list {
    list-style-type: none;
    padding-left: 0;
    column-count: 2;
    column-gap: 40px;
}

.ingredient-item {
    margin-bottom: 12px;
    break-inside: avoid;
    position: relative;
    padding-left: 28px;
    font-size: 11pt;
}

/* Small checkbox for print utility */
.ingredient-item:before {
    content: "";
    position: absolute;
    left: 0;
    top: 3px;
    width: 14px;
    height: 14px;
    border: 1px solid var(--primary-color);
    border-radius: 3px;
}

.quantity {
    font-weight: bold;
    color: #000;
}

/* --- Instructions --- */
.instructions-list {
    padding-left: 0;
    list-style: none;
    counter-reset: steps;
}

.instruction-item {
    margin-bottom: 20px;
    padding-left: 45px;
    position: relative;
    break-inside: avoid;
}

.instruction-item:before {
    counter-increment: steps;
    content: counter(steps);
    position: absolute;
    left: 0;