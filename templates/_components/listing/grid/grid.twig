<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-5 {{ featured|default(false) ? 'grid-featured' }}">
    {% for item in items %}
        {% include item.type with item.data|merge(featured|default(false) and loop.first ? { large: true } : {}) %}
    {% endfor %}
</div>
